// Class representing difficult Computer version

class HardComputer extends Computer
{

    calculateMove(spaces, coordinates)
    {
        // calculate strategic move
        const analyzedSpaces = new Array();
        spaces.forEach(space => {
            // analyze each space for how good a move it would be
            // push an object containing the rating and the id to analyzed spaces
        });

        // get the id for the highest or best rated space
        // return the id

        return move;
    }


    move(spaces, coordinates)
    {
        // execute move and present visually
        const move = calculateMove(spaces, coordinates);

        // update space object
        // draw piece in space on the board
    }
}
